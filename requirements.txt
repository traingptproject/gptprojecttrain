transformers==4.57.1
datasets==4.4.1
peft==0.18.0
bitsandbytes==0.48.2
accelerate>=0.33.0
# On Colab, torch is usually preinstalled. If needed, install CUDA-enabled torch matching the runtime.

