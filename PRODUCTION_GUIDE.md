# 🚀 คู่มือการใช้งาน dLNk GPT Production Training System

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การเตรียมความพร้อม](#การเตรียมความพร้อม)
3. [ขั้นตอนการใช้งาน](#ขั้นตอนการใช้งาน)
4. [การตั้งค่า Telegram](#การตั้งค่า-telegram)
5. [ระบบป้องกันปัญหา](#ระบบป้องกันปัญหา)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
7. [คำถามที่พบบ่อย](#คำถามที่พบบ่อย)

---

## ภาพรวมระบบ

### ✨ ฟีเจอร์หลัก

**ระบบดึงข้อมูลอัตโนมัติ:**
- ✅ Exploit-DB (50,000+ exploits)
- ✅ PayloadsAllTheThings (10,000+ payloads)
- ✅ SecLists (100,000+ fuzzing payloads)

**ระบบป้องกันปัญหา:**
- ✅ Overfitting Detection - ตรวจจับ overfitting อัตโนมัติ
- ✅ Learning Progress Monitoring - ติดตามการเรียนรู้ของโมเดล
- ✅ Data Conflict Detection - ตรวจสอบข้อมูลที่ขัดแย้ง
- ✅ Auto Learning Rate Adjustment - ปรับ learning rate อัตโนมัติ

**การแจ้งเตือน:**
- ✅ แจ้งเตือนภาษาไทยผ่าน Telegram แบบเรียลไทม์
- ✅ รายงานความคืบหน้าทุก epoch
- ✅ แจ้งเตือนเมื่อพบปัญหา

**การทำงาน:**
- ✅ รัน Test Mode ก่อนอัตโนมัติ (2-3 นาที)
- ✅ เทรนบนข้อมูลจริงหลังจาก Test ผ่าน
- ✅ บันทึกผลลัพธ์ไปยัง Hugging Face และ W&B

---

## การเตรียมความพร้อม

### 1. สิ่งที่ต้องมี

**บัญชีและ API Keys:**
- [ ] Google Account (สำหรับ Colab)
- [ ] Hugging Face Account + Token
- [ ] Weights & Biases Account + API Key (Optional)
- [ ] Telegram Bot Token + Chat ID

**ฮาร์ดแวร์:**
- GPU: T4, V100, หรือ A100 (แนะนำ A100)
- RAM: อย่างน้อย 12GB
- Storage: อย่างน้อย 20GB ใน Google Drive

### 2. การหา API Keys

หากคุณลืม API Keys หรือต้องการสร้างใหม่ กรุณาดูคู่มือที่:
- **[API_KEY_GUIDE.md](API_KEY_GUIDE.md)** - คู่มือการค้นหา API Keys ฉบับสมบูรณ์

---

## ขั้นตอนการใช้งาน

### ขั้นตอนที่ 1: เปิด Colab Notebook

1. ไปที่ GitHub Repository:
   ```
   https://github.com/traingptproject/gptprojecttrain
   ```

2. คลิกที่ไฟล์ `dLNk_GPT_Production_Complete.ipynb`

3. คลิกปุ่ม "Open in Colab" (หรือคัดลอก URL ไปเปิดใน Colab)

### ขั้นตอนที่ 2: เปลี่ยน Runtime เป็น GPU

1. ไปที่เมนู `Runtime` → `Change runtime type`
2. เลือก `Hardware accelerator`: **GPU**
3. เลือก `GPU type`: **T4** (หรือ A100 ถ้ามี Colab Pro)
4. คลิก "Save"

### ขั้นตอนที่ 3: ใส่ API Keys และ Tokens

**Cell ที่ 4: Hugging Face Token**
```python
HF_TOKEN = "YOUR_HF_TOKEN_HERE"  # 👈 ใส่ token ของคุณที่นี่
```

**Cell ที่ 5: Weights & Biases API Key (Optional)**
```python
WANDB_API_KEY = ""  # 👈 ใส่ W&B API key ที่นี่ (หรือเว้นว่างไว้)
```

**Cell ที่ 7: Telegram**
```python
TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN"  # 👈 ใส่ Bot Token
TELEGRAM_CHAT_ID = "YOUR_CHAT_ID"      # 👈 ใส่ Chat ID
```

### ขั้นตอนที่ 4: รัน Cells ทั้งหมด

1. ไปที่เมนู `Runtime` → `Run all`
2. กดอนุญาตเมื่อมี popup ขึ้นมา (สำหรับ Mount Google Drive)
3. รอให้ระบบทำงาน

### ขั้นตอนที่ 5: ติดตามความคืบหน้า

**ผ่าน Colab:**
- ดูผลลัพธ์ใน cells
- ตรวจสอบ GPU usage

**ผ่าน Telegram:**
- รับการแจ้งเตือนทุก epoch
- รับรายงานปัญหา (ถ้ามี)

**ผ่าน Weights & Biases:**
- เข้าไปดูที่ https://wandb.ai
- ดู charts และ metrics แบบเรียลไทม์

---

## ระบบป้องกันปัญหา

### 1. Overfitting Detection

**อาการ:**
- Train loss ลดลง แต่ Eval loss เพิ่มขึ้น
- Gap ระหว่าง train และ eval loss มากกว่า 0.5

**การแก้ไขอัตโนมัติ:**
- ระบบจะแจ้งเตือนผ่าน Telegram
- แนะนำให้เพิ่ม dropout หรือ weight decay
- พิจารณาหยุดการเทรนก่อนกำหนด

### 2. Learning Progress Monitoring

**อาการ:**
- Loss ไม่ลดลงใน 3 epochs ติดต่อกัน
- โมเดลหยุดเรียนรู้

**การแก้ไขอัตโนมัติ:**
- ระบบจะลด learning rate อัตโนมัติ
- แจ้งเตือนผ่าน Telegram
- แนะนำให้ตรวจสอบข้อมูล

### 3. Data Conflict Detection

**อาการ:**
- Instruction + Input เดียวกัน แต่มี Output ต่างกัน
- โมเดลสับสน

**การแก้ไขอัตโนมัติ:**
- ระบบจะรายงานข้อมูลที่ขัดแย้ง
- แจ้งจำนวนและตัวอย่าง
- แนะนำให้ทำความสะอาดข้อมูล

### 4. Auto Learning Rate Adjustment

**เงื่อนไข:**
- ถ้า loss ไม่ลดลง → ลด LR เหลือ 50%
- ถ้า loss ลดลงเร็วมาก → เพิ่ม LR ขึ้น 20%
- ไม่เกิน 5e-5

---

## การแก้ไขปัญหา

### ปัญหา: GPU Out of Memory

**สาเหตุ:**
- Batch size ใหญ่เกินไป
- โมเดลใหญ่เกินไป

**วิธีแก้:**
1. ลด `per_device_train_batch_size` ใน training config
2. เปิดใช้ `gradient_accumulation_steps`
3. ใช้ `fp16=True` หรือ `bf16=True`

**ตัวอย่าง:**
```python
training_args = TrainingArguments(
    per_device_train_batch_size=1,  # ลดจาก 4 เป็น 1
    gradient_accumulation_steps=4,   # เพิ่มเป็น 4
    bf16=True,                        # ใช้ bfloat16
)
```

### ปัญหา: Telegram ไม่ส่งข้อความ

**สาเหตุ:**
- Chat ID ผิด
- Bot Token ผิด
- ยังไม่ได้กด Start ที่ Bot

**วิธีแก้:**
1. ตรวจสอบ Chat ID อีกครั้ง (ต้องเป็นตัวเลข)
2. ตรวจสอบ Bot Token
3. เปิด Telegram และกด Start ที่ Bot
4. รัน cell ที่ 7 อีกครั้งเพื่อทดสอบ

### ปัญหา: ดาวน์โหลดข้อมูลช้า

**สาเหตุ:**
- อินเทอร์เน็ตช้า
- Repository ใหญ่

**วิธีแก้:**
1. รอให้ดาวน์โหลดเสร็จ (ครั้งแรกใช้เวลา 10-15 นาที)
2. ครั้งต่อไปจะเร็วขึ้น (ใช้ cache)
3. หรือดาวน์โหลดไว้ใน Google Drive แล้ว mount

### ปัญหา: Colab Disconnect

**สาเหตุ:**
- ปิด browser tab
- Idle นานเกินไป

**วิธีแก้:**
1. เปิด tab ไว้ตลอด (ปิด browser ได้ แต่เปิด tab)
2. ใช้ Colab Pro สำหรับ runtime ที่ยาวขึ้น
3. ใช้ script ป้องกัน idle:
   ```javascript
   function KeepAlive() {
     console.log("Keeping alive");
     document.querySelector("colab-toolbar-button#connect").click();
   }
   setInterval(KeepAlive, 60000);
   ```
   (รันใน Browser Console)

---

## คำถามที่พบบ่อย

### Q1: ใช้เวลาเทรนนานแค่ไหน?

**A:** ขึ้นอยู่กับ GPU:
- **T4:** 3-4 ชั่วโมง
- **V100:** 2-3 ชั่วโมง
- **A100:** 1-2 ชั่วโมง

### Q2: ต้องใช้ Colab Pro หรือไม่?

**A:** ไม่จำเป็น แต่แนะนำเพราะ:
- GPU ที่ดีกว่า (V100, A100)
- Runtime ที่ยาวขึ้น
- ไม่มีคิวรอ

### Q3: ข้อมูลจะถูกเก็บไว้ที่ไหน?

**A:** 
- **Training data:** Google Drive (`/content/drive/MyDrive/Colab Notebooks/`)
- **Model checkpoints:** Google Drive + Hugging Face Hub
- **Logs:** Weights & Biases

### Q4: สามารถหยุดและเทรนต่อได้หรือไม่?

**A:** ได้ โดย:
1. Checkpoints จะถูกบันทึกทุก epoch
2. ใช้ `resume_from_checkpoint=True` ในครั้งต่อไป
3. ระบบจะเทรนต่อจากจุดที่หยุด

### Q5: โมเดลที่เทรนแล้วอยู่ที่ไหน?

**A:**
- **Hugging Face Hub:** `https://huggingface.co/dLNk/gpt-j-6b-exploit-v2`
- **Google Drive:** `/content/drive/MyDrive/Colab Notebooks/models/`
- **Weights & Biases:** Artifacts section

### Q6: สามารถเทรนด้วยข้อมูลของตัวเองได้หรือไม่?

**A:** ได้ โดย:
1. เตรียมข้อมูลในรูปแบบ JSONL:
   ```json
   {"instruction": "...", "input": "...", "output": "..."}
   ```
2. อัปโหลดไปยัง Google Drive
3. แก้ไข path ใน cell ที่ 9

### Q7: ต้องการความช่วยเหลือเพิ่มเติม?

**A:** ติดต่อได้ที่:
- GitHub Issues: https://github.com/traingptproject/gptprojecttrain/issues

---

## สรุป

### สิ่งที่คุณได้รับ:

✅ **Colab Notebook ฉบับสมบูรณ์** - พร้อมใช้งานทันที  
✅ **ระบบดึงข้อมูลอัตโนมัติ** - จาก 3 แหล่งหลัก  
✅ **ระบบป้องกันปัญหา** - ครบวงจร 4 ระบบ  
✅ **การแจ้งเตือนเรียลไทม์** - ภาษาไทยผ่าน Telegram  
✅ **Test Mode** - ทดสอบก่อนเทรนจริงอัตโนมัติ  
✅ **Integration** - W&B และ Hugging Face  

### ขั้นตอนสรุป:

1. เตรียม API Keys และ Tokens
2. เปิด Colab Notebook
3. ใส่ Keys และ Tokens
4. รัน cells ทั้งหมด
5. รอรับการแจ้งเตือนผ่าน Telegram
6. ตรวจสอบผลลัพธ์ใน W&B
7. ดาวน์โหลดโมเดลจาก Hugging Face

---

**สร้างโดย:** dLNk GPT Training System  
**เวอร์ชัน:** Production v1.0  
**วันที่อัปเดต:** พฤศจิกายน 2025
