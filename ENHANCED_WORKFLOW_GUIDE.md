# dLNk GPT - Enhanced Workflow Guide

**Version:** 1.0
**Date:** 15 พฤศจิกายน 2025

---

## 🚀 ภาพรวม

เอกสารนี้อธิบาย **Enhanced Workflow** ซึ่งเป็นระบบอัตโนมัติที่ทำงาน **หลังจากการเทรน V2 เสร็จสิ้น** เพื่อเพิ่มความสามารถ 3 ด้านหลักให้กับโมเดล:

1. **Real-time Information Retrieval** - ผ่าน RAG (Retrieval-Augmented Generation)
2. **External Knowledge Integration** - เชื่อมต่อกับ APIs, databases, และ code execution
3. **Advanced Code Generation** - เพิ่มความสามารถในการเขียน, ทดสอบ, และอธิบายโค้ด

**เป้าหมาย:** สร้าง AI assistant ที่มีความสามารถรอบด้าน, เข้าถึงข้อมูล real-time, และทำงานที่ซับซ้อนได้

---

## ⚙️ สถาปัตยกรรม

Enhanced Workflow ประกอบด้วย 4 ส่วนหลัก:

### 1. **Enhanced Inference System (`enhanced_inference.py`)**

- **Core system** ที่รวมทุกอย่างเข้าด้วยกัน
- โหลดโมเดลที่เทรนแล้ว (V2)
- จัดการ RAG และ external knowledge
- สร้าง prompt และประมวลผลคำตอบ

### 2. **RAG System (`rag_system.py`)**

- **Real-time information retrieval**
- ค้นหาข้อมูลจาก:
  - Web (DuckDuckGo)
  - Wikipedia
  - GitHub
  - Stack Overflow
- สร้าง vector index (FAISS) สำหรับ caching

### 3. **Knowledge Integration (`knowledge_integration.py`)**

- **เชื่อมต่อกับ external tools**
- **Code Execution:** รัน Python และ Bash code ใน sandbox
- **Database:** เก็บและค้นหาข้อมูลใน SQLite
- **Real-time Data:** ดึงข้อมูล crypto, GitHub trending, tech news

### 4. **Automated Workflow (`run_post_training.py`)**

- **Script หลัก** ที่รันทุกอย่างอัตโนมัติ
- ตรวจสอบว่าการเทรนเสร็จแล้ว
- ติดตั้ง dependencies
- ทดสอบทุกระบบ
- สร้าง API server และ usage examples

---

## 🛠️ การทำงาน

### ขั้นตอนที่ 1: การเทรน V2 (กำลังดำเนินการ)

- โมเดลกำลังเรียนรู้จากข้อมูล 999,972 samples
- หลังจากเทรนเสร็จ จะได้โมเดลที่ `dLNk-gpt-uncensored-v2`

### ขั้นตอนที่ 2: Post-Training Enhancement (อัตโนมัติ)

หลังจากเทรนเสร็จ `run_post_training.py` จะทำงานอัตโนมัติ:

1. **ตรวจสอบ:** เช็คว่าโมเดล V2 มีอยู่จริง
2. **ติดตั้ง:** ติดตั้ง `sentence-transformers`, `faiss-cpu`, `requests`
3. **ทดสอบ:**
   - **RAG System:** ทดสอบการค้นหา Wikipedia
   - **Knowledge Integration:** ทดสอบ code execution และ real-time data
   - **Enhanced Inference:** ทดสอบการโหลดโมเดลและ generate response
4. **สร้าง:**
   - `api_server.py`: API server สำหรับใช้งานโมเดล
   - `usage_examples.py`: ตัวอย่างการใช้งาน
5. **รายงาน:** สร้าง `enhancement_report.json` สรุปผล

### ขั้นตอนที่ 3: การใช้งาน

หลังจาก enhancement เสร็จ คุณสามารถ:

#### 1. **ใช้งานผ่าน API Server**

- **Start server:**
  ```bash
  python api_server.py
  ```
- **Send request:**
  ```bash
  curl -X POST -H "Content-Type: application/json" \
       -d '{"query": "What is the current price of Bitcoin?"}' \
       http://localhost:5000/generate
  ```

#### 2. **ใช้งานผ่าน Python Script**

- **Run examples:**
  ```bash
  python usage_examples.py
  ```
- **Integrate in your code:**
  ```python
  from enhanced_inference import EnhancedInference

  inference = EnhancedInference()
  response = inference.chat("Write a Python web scraper")
  print(response)
  ```

---

## 🎯 ความสามารถที่เพิ่มขึ้น

### 1. Real-time Information

**คำถาม:** "What is the latest version of Python?"

**การทำงาน:**
1. `EnhancedInference` ตรวจจับ keyword "latest"
2. `RAGSystem` ค้นหา web และ Wikipedia
3. ข้อมูลที่ได้จะถูกใส่ใน prompt
4. โมเดลจะตอบโดยใช้ข้อมูล real-time

### 2. Code Execution

**คำถาม:** "Run this Python code: `print(2+2)`"

**การทำงาน:**
1. `EnhancedInference` ตรวจจับ keyword "Run this"
2. `KnowledgeIntegration` ใช้ `CodeExecutor` รันโค้ด
3. ผลลัพธ์ (stdout, stderr) จะถูกส่งกลับ
4. โมเดลจะอธิบายผลลัพธ์

### 3. External Knowledge

**คำถาม:** "What are the trending Python repositories on GitHub?"

**การทำงาน:**
1. `EnhancedInference` ตรวจจับ keyword "trending"
2. `KnowledgeIntegration` ใช้ `RealtimeDataFetcher` ดึงข้อมูลจาก GitHub API
3. ข้อมูลที่ได้จะถูกใส่ใน prompt
4. โมเดลจะสรุปและตอบ

### 4. Advanced Code Generation

**คำถาม:** "Write a Flask app to show Bitcoin price"

**การทำงาน:**
1. **RAG:** ค้นหา Flask examples และ Bitcoin API
2. **Knowledge Integration:** ดึงราคา Bitcoin
3. **Model:** สร้างโค้ด Flask ที่รวมข้อมูล real-time
4. **Code Execution:** (Optional) ทดสอบโค้ด

-
### 3. **ประสิทธิภาพ**

- **RAG และ external calls** จะทำให้ response ช้าลง
- **Caching (Redis)** ช่วยลด latency ในการเรียกซ้ำ
- **GPU** จำเป็นสำหรับ performance ที่ดี

---

## 🚀 สรุป

Enhanced Workflow นี้จะเปลี่ยนโมเดลของคุณจาก **language model** ธรรมดาให้กลายเป็น **AI assistant ที่มีความสามารถรอบด้าน**

**ความสามารถหลัก:**
- **Reasoning:** จากโมเดลที่เทรนมา
- **Knowledge:** จาก RAG และ external APIs
- **Action:** จาก code execution และ database integration

**ผลลัพธ์:** โมเดลที่สามารถ:
- ตอบคำถามที่ต้องการข้อมูล real-time
- ทำงานที่ซับซ้อน (เช่น เขียนโค้ด, ทดสอบ, อธิบาย)
- เชื่อมต่อกับระบบภายนอก
- เรียนรู้และปรับปรุงตัวเองผ่าน knowledge base

---

**เอกสารนี้จะถูก commit ไปที่ GitHub พร้อมกับโค้ดทั้งหมด**
